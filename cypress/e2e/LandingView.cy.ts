/*describe('LandingView tests', () => {
  it('passes', () => {
    cy.visit('/')
    cy.contains('h1', 'Velkommen!')
    
    cy.intercept('/dashboard').as("dashboard");
    cy.get('button').contains("Fortsett").click();
    cy.url().should('include', '/dashboard');

  })
})
*/